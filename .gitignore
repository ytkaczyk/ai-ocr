**/*/launch.json
**/*/.env

# Ignore all subdirectory with /examples/
examples/*/
# Snyk Security Extension - AI Rules (auto-generated)
.github/instructions/snyk_rules.instructions.md

# snyk config
.snyk

# Node dependencies. apps/web-viewer/.gitignore anchors its rule with a leading
# slash, so it only covers that app; without this a stray `npm install` at the
# repo root leaves the whole tree unignored.
node_modules/
